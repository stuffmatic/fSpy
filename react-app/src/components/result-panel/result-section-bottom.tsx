import * as React from 'react';
import Button from './../common/button'

export default class ResultSectionBottom extends React.PureComponent {
  render() {
    return (
      <div className="panel-section top-border">
        <Button title={"Export"} onClick={() => { }} />
      </div>
    )
  }
}