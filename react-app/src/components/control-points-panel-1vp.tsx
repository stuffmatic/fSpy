/*import * as React from 'react';
import ControlPointsPanel, { ControlPointsPanelProps } from './control-points-panel';
import HorizonControl from './horizon-control'

function ControlPointsPanel1VP(props: ControlPointsPanelProps) {
  return (
    <ControlPointsPanel {...props}>
      <HorizonControl
        enabled={true}
        start={{ x: props.width * 0.1, y: props.height * 0.1 }}
        end={{ x: props.width * 0.5, y: props.height * 0.5 }}
        startDragCallback={(x: number, y: number) => { }}
        endDragCallback={(x: number, y: number) => { }}
      />
    </ControlPointsPanel>
  )
}

export default ControlPointsPanel1VP*/